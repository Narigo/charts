{
  "name": "charts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "node into-charts.js && npm run format",
    "crawl": "node crawler.js 1953 $(date +'%Y')",
    "format": "prettier --write '*.js' && prettier --write 'results/*.json' && prettier --write 'docs/**/*.js'",
    "test": "npm run test:unit && npm run test:lint",
    "test:unit": "jest",
    "test:lint": "eslint --max-warnings=0 '**/*.js'"
  },
  "keywords": [],
  "author": "Joern Bernhardt <joern.bernhardt@googlemail.com> (http://www.unreleased.de)",
  "license": "MIT",
  "devDependencies": {
    "@babel/plugin-transform-modules-commonjs": "^7.6.0",
    "babel-eslint": "^10.0.3",
    "eslint": "^6.5.1",
    "jest": "^24.9.0",
    "prettier": "^1.18.2",
    "puppeteer": "^1.20.0"
  }
}
